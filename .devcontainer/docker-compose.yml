services:
  opadviser:
    build:
      additional_contexts:
         parent: ..
      context: .
      dockerfile: Dockerfile
    cgroup: host
    command:
      - sleep infinity
    privileged: true
    shm_size: 4gb
    volumes:
      - /mnt/sdc/jeseok2/opadviser:/opadviser
      - /mnt/sdc/jeseok2/mysql:/var/lib/mysql
