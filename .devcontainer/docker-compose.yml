services:
  opadviser:
    build:
      context: .
      dockerfile: Dockerfile
    cgroup: host
    command:
      - sleep infinity
    privileged: true
    shm_size: 4gb
    volumes:
      - /mnt/sdc/jeseok/opadviser:/opadviser:delegated
      - /mnt/sdc/jeseok/mysql2:/var/lib/mysql:delegated
