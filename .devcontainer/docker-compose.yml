services:
  opadviser:
    build:
      context: .
      dockerfile: Dockerfile
    command:
      - sleep infinity
    shm_size: 4gb
    volumes:
      - /mnt/sdd/jeseok/opadviser:/
  db:
    environment:
      MYSQL_ROOT_PASSWORD: password
    image: "mysql:5.7"
    ports:
      - "3306:3306"
    volumes:
      - /mnt/sdd/jeseok/opadviser/var/lib/mysql:/var/lib/mysql
      - /mnt/sdd/jeseok/opadviser/var/run/mysqld:/var/run/mysqld